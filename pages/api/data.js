const URL='https://wpjixgfnynrboptwpotd.supabase.co';const KEY='sb_publishable_IwnnE09a7GnBzIHG-Z4ssQ_gTJTqLfg';const stageDb={Sourcing:'sourcing','CV Review':'cv_review','HR Call':'hr_call','HR Interview':'hr_interview','Technical Interview':'technical_interview',Offer:'offer',Hired:'hired',Rejected:'rejected','On Hold':'on_hold','Talent Pool':'talent_pool'};
function extractContact(text=''){const normalized=text.replace(/[\u200c\u200f\u202a-\u202e]/g,' ');const email=(normalized.match(/[A-Z0-9._%+-]+@[A-Z0-9.-]+\.[A-Z]{2,}/i)||[])[0]||'';const phones=normalized.match(/(?:\+?98|0098|0)?[\s\-()]?9\d{2}[\s\-()]?\d{3}[\s\-()]?\d{4}/g)||[];let phone=(phones[0]||'').replace(/[^\d+]/g,'');if(phone.startsWith('0098'))phone='+98'+phone.slice(4);else if(phone.startsWith('98')&&!phone.startsWith('+'))phone='+'+phone;else if(/^9\d{9}$/.test(phone))phone='0'+phone;return{email,phone}}
async function refresh(refreshToken){if(!refreshToken)return null;const r=await fetch(`${URL}/auth/v1/token?grant_type=refresh_token`,{method:'POST',headers:{apikey:KEY,'Content-Type':'application/json'},body:JSON.stringify({refresh_token:refreshToken})});const d=await r.json().catch(()=>({}));if(!r.ok)throw new Error(d?.msg||d?.error_description||d?.message||'Session expired');return d}
function expired(token){try{const p=JSON.parse(Buffer.from((token||'').split('.')[1]||'','base64url').toString());return !p.exp||p.exp*1000<Date.now()+15000}catch{return true}}
async function raw(path,{method='GET',token,body,prefer}={}){const r=await fetch(`${URL}/rest/v1/${path}`,{method,headers:{apikey:KEY,Authorization:`Bearer ${token}`,'Content-Type':'application/json',...(prefer?{Prefer:prefer}:{})},body:body?JSON.stringify(body):undefined});const text=await r.text();let data=null;try{data=text?JSON.parse(text):null}catch{data=text}return{r,data}}
async function ensureFresh(ctx){if((!ctx.token||expired(ctx.token))&&ctx.refreshToken){const fresh=await refresh(ctx.refreshToken);ctx.token=fresh.access_token;ctx.refreshToken=fresh.refresh_token||ctx.refreshToken;ctx.user=fresh.user||ctx.user;ctx.refreshed={access_token:ctx.token,refresh_token:ctx.refreshToken,user:ctx.user}}}
async function sb(path,opts,ctx){await ensureFresh(ctx);let out=await raw(path,{...opts,token:ctx.token});if(out.r.status===401&&ctx.refreshToken){const fresh=await refresh(ctx.refreshToken);ctx.token=fresh.access_token;ctx.refreshToken=fresh.refresh_token||ctx.refreshToken;ctx.user=fresh.user||ctx.user;ctx.refreshed={access_token:ctx.token,refresh_token:ctx.refreshToken,user:ctx.user};out=await raw(path,{...opts,token:ctx.token})}if(!out.r.ok)throw new Error(out.data?.message||out.data?.hint||out.data?.details||`Database error ${out.r.status}`);return out.data}
export default async function handler(req,res){const token=(req.headers.authorization||'').replace(/^Bearer\s+/i,'');const refreshToken=req.headers['x-refresh-token']||req.body?.refreshToken||'';if(!token&&!refreshToken)return res.status(401).json({error:'Missing session token',code:'AUTH_REQUIRED'});const ctx={token,refreshToken,user:null,refreshed:null};const{op}=req.body||{};try{await ensureFresh(ctx);let result;if(op==='load'){const[requests,jobs,candidates,activities,comments]=await Promise.all([sb('hiring_requests?select=*&order=created_at.desc',{},ctx),sb('jobs?select=*&order=created_at.desc',{},ctx),sb('candidates?select=*&order=created_at.desc',{},ctx),sb('activities?select=*&order=created_at.desc',{},ctx),sb('candidate_comments?select=*&order=created_at.desc',{},ctx)]);result={requests,jobs,candidates,activities,comments}}else if(op==='create_request'){const f=req.body.form||{};const rows=await sb('hiring_requests',{method:'POST',prefer:'return=representation',body:{created_by:req.body.userId,title:f.title,department:f.department,hiring_manager_name:f.manager||null,openings:Number(f.hc)||1,reason:'Growth / Replacement',business_justification:'Submitted via Recruitment OS',job_description:f.jd,responsibilities:f.jd,must_have:f.must||f.jd,seniority:'Not specified'}},ctx);result={request:rows?.[0]}}else if(op==='approve_request'){const id=req.body.id,userId=req.body.userId;const rows=await sb(`hiring_requests?id=eq.${encodeURIComponent(id)}`,{method:'PATCH',prefer:'return=representation',body:{status:'approved',approved_by:userId,approved_at:new Date().toISOString()}},ctx);const r=rows?.[0];if(!r)throw new Error('Request not found');const js=await sb('jobs',{method:'POST',prefer:'return=representation',body:{hiring_request_id:r.id,title:r.title,department:r.department,hiring_manager_name:r.hiring_manager_name,openings:r.openings,employment_type:r.employment_type,work_model:r.work_model,priority:r.priority,target_hiring_date:r.target_hiring_date,job_description:r.job_description,responsibilities:r.responsibilities,must_have:r.must_have,nice_to_have:r.nice_to_have,seniority:r.seniority,minimum_experience:r.minimum_experience,education_requirement:r.education_requirement,salary_from:r.salary_from,salary_to:r.salary_to,status:'open'}},ctx);result={request:r,job:js?.[0]}}else if(op==='reject_request'){const rows=await sb(`hiring_requests?id=eq.${encodeURIComponent(req.body.id)}`,{method:'PATCH',prefer:'return=representation',body:{status:'rejected',approved_by:req.body.userId,approved_at:new Date().toISOString()}},ctx);result={request:rows?.[0]}}else if(op==='create_candidate'){const c=req.body.candidate||{},auto=extractContact(c.resumeText||'');const rows=await sb('candidates',{method:'POST',prefer:'return=representation',body:{job_id:req.body.jobId,full_name:c.name,email:c.email||auto.email||null,phone:c.phone||auto.phone||null,stage:'cv_review',hr_approval:'pending',resume_text:c.resumeText||null,resume_file_name:c.fileName||null,screening_score:c.score,screening_summary:c.why,created_by:req.body.userId}},ctx);const row=rows?.[0];if(row)await Promise.all([sb('activities',{method:'POST',body:{candidate_id:row.id,actor_id:req.body.userId,action:'resume_screened',metadata:{text:`Resume uploaded and screened — score ${c.score}`}}},ctx),sb('activities',{method:'POST',body:{candidate_id:row.id,actor_id:req.body.userId,action:'stage_changed',metadata:{to:'cv_review',text:'Added to CV Review'}}},ctx)]);result={candidate:row,contact:{email:row?.email||auto.email||'',phone:row?.phone||auto.phone||''}}}else if(op==='move_candidate'){const stage=stageDb[req.body.stage]||req.body.stage;const rows=await sb(`candidates?id=eq.${encodeURIComponent(req.body.candidateId)}`,{method:'PATCH',prefer:'return=representation',body:{stage}},ctx);await sb('activities',{method:'POST',body:{candidate_id:req.body.candidateId,actor_id:req.body.userId,action:'stage_changed',metadata:{to:stage,text:`Stage changed to ${req.body.stage}`}}},ctx);result={candidate:rows?.[0]}}else if(op==='hr_decision'){const approved=!!req.body.approved,stage=approved?'hr_call':'rejected',approval=approved?'approved':'rejected';const rows=await sb(`candidates?id=eq.${encodeURIComponent(req.body.candidateId)}`,{method:'PATCH',prefer:'return=representation',body:{stage,hr_approval:approval}},ctx);await sb('activities',{method:'POST',body:{candidate_id:req.body.candidateId,actor_id:req.body.userId,action:'hr_decision',metadata:{approved,text:approved?'HR approved candidate → HR Call':'HR rejected candidate'}}},ctx);result={candidate:rows?.[0]}}else if(op==='add_comment'){const rows=await sb('candidate_comments',{method:'POST',prefer:'return=representation',body:{candidate_id:req.body.candidateId,author_id:req.body.userId,body:req.body.body}},ctx);await sb('activities',{method:'POST',body:{candidate_id:req.body.candidateId,actor_id:req.body.userId,action:'comment_added',metadata:{text:'New team comment added'}}},ctx);result={comment:rows?.[0]}}else return res.status(400).json({error:'Unknown operation'});return res.json({...result,...(ctx.refreshed?{session:ctx.refreshed}:{})})}catch(e){const m=e.message||'Database operation failed';const auth=/jwt|session|token|expired|refresh/i.test(m);return res.status(auth?401:500).json({error:m,code:auth?'SESSION_EXPIRED':'DATA_ERROR'})}}